{% extends "base.jinja2" %}

{% block title %}Portfolio App{% endblock %}
{% block content %}

<div class="section">
    <div class="row container">
        <h2 class="header white-text">Serviços</h2>
        <div class="row">
            {% for service_title, service_items in services %}
                <div class="card grey darken-3">
                    <div class="card-content white-text">
                        <span class="card-title">{{ service_title }}</span>
                        <div class="divider"></div>
                        {% for service_item in service_items %}
                            <p>{{ service_item }}</p>
                        {% endfor %}
                    </div>
                </div>
            {% endfor %}
        </div> 
    </div>
</div>    
{% endblock %}